server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;  # fallback for React Router
    }
    location /api {
        proxy_pass http://backend:8080;  # K8s service name
    }
}