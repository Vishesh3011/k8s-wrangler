{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html app.js config.js dist/ || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}